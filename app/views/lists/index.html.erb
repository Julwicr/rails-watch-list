<h1>Lists#index</h1>

<div class="container">
<% @lists.each do |list| %>
  <div class="card">
  <%= link_to list.name, list_path(list) %>
  </div>
<% end %>
</div>
<%= link_to 'Create list', new_list_path %>
